# Lean / Lake — any workspace, at any depth. `physics/` is a second Lake workspace and a
# path-anchored rule missed it, so its whole dependency tree (Mathlib included) got
# committed once.
.lake/

# Rust
target/

# Editors
.vscode/
.idea/

# Generated by `fh_emit` for the round-trip gate (scripts/round-trip.py)
lean/Tests/Emitted/

# Python
__pycache__/
*.pyc
.venv/
venv/
