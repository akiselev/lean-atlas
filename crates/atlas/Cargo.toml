[package]
name = "atlas"
description = "Semantic indexes over elaborated Lean environments (see atlas.md)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
atlas-schema = { path = "../atlas-schema" }
sha2 = "0.11.0"
